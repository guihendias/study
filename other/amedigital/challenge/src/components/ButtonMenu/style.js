import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
  buttonMenu: {
    height: 60,
    width: 40,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
